<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login form</title>
    <link rel="stylesheet" href="/register.css" type="text/css">
</head>

<body>
    <div class="outer ">
        <div class="container">
            <h3>update your information</h3>
            <div class="list">
            </div>
            {{#if serverSuccess}}
            <div class="success">{{serverSuccess}}</div>
            {{/if}}
            {{#if serverError}}
            <div class="error">{{serverError}}</div>
            {{/if}}
            <div class="s">
                <form action="/log/update/{{user.username}}/up" method="POST" class="signup" >
                    <input type="text" placeholder="First name" name="firstname" id="first" value="{{user.firstname}}" required>
                    <input type="text" placeholder="Last name" name="lastname" id="last" value="{{user.lastname}}" required></br>
                    <input type="email" name="email" id="email" name="email" placeholder="Email" value="{{user.email}}" required>
                    <input type="text" placeholder="Username" id="username" name="username" value="{{user.username}}" required></br>
                    <input type="password" placeholder="password" id="password" name="password" required>
                    <input type="password" placeholder="confirm password" id="cpassword" name="cpassword" required></br>
                    <input type="submit" value="Update" id="signup"></br>
                </form>
            </div>
        </div>
    </div>
</body>

</html>